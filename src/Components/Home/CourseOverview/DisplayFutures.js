import React from 'react';
import { useParams } from 'react-router-dom';

const DisplayFutures = (props) => {
 const {courses} =useParams()
 return (
  <div>

  </div>
 );
};

export default DisplayFutures;